# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- 新增 Nginx + Gunicorn + systemd 生产部署指南及可直接复用的服务、反向代理、环境变量模板，覆盖宝塔注意事项、uv 锁定环境、HTTPS、Basic Auth、备份、升级和故障排查
- 运行日志新增连续行号、复制当前日志和全屏查看按钮
- 新增“简略快速”查询模式：保留注册状态与解析/封禁判断所需查询，限制为一次 WHOIS 尝试和一次 DNS 检查，跳过 HTTP 探测与 DNS 自动复查
- 查询开始即展示全部域名，并在状态列实时区分排队中、正在查询和最终查询结果；新增已注册/未注册结论列，原 WHOIS 状态列更名为域名状态
- 解析列默认移至注册状态右侧；结果列支持鼠标拖拽换序并持久化，正在查询状态增加固定尺寸加载动画
- 结果展示可在“状态刷新”和“逐条加入”间即时切换并保存偏好，沿用当前字段顺序与列设置
- 新增独立“使用说明”页面，以及分步骤展示的 Windows/macOS/Linux 端口排查与停止文档；网页不暴露停服接口
- 新增持久化服务操作日志，展示服务启动与正常退出记录
- 服务操作日志移至独立主板块；查询历史新增复选批量删除和清理全部，并阻止删除运行中任务
- 查询结果新增 WHOIS/RDAP 联系邮箱字段并贯穿历史与导出；表格支持拖拽调整并保存列宽，点击普通单元格即可复制
- 查询结果表格的域名旁新增复制按钮，支持复制成功反馈与旧浏览器回退
- 查询和重新查询期间新增加载动画、阶段进度与实时/最终耗时，完成状态保持可见
- 运行日志增加并发参数、WHOIS/DNS 阶段、重试等待和单域名耗时，并改为增量拉取
- 页面大纲改为 VS Code 风格活动侧栏，颜色跟随主题，支持左右停靠、紧凑折叠与当前区块定位；
  运行日志、查询结果等条件区块仅在可用时显示菜单
- 域名输入框增加同步滚动的行号，并提供知名网站、混合域名和 URL 三组演示预设
- 移动端侧栏改为支持左右方向的汉堡菜单抽屉，并补充遮罩、Esc/菜单项关闭及响应式页面布局
- 新增 `spec/` 渐进式 Coding Agent 规格入口、查询与前端契约、开发流程和交接规范

### 变更（性能与行为）

- 暂停或终止任务时，状态列会将尚未完成的行标记为“已暂停”或“已终止”；已完成行保持原查询结果，恢复后回到暂停前阶段
- 暂停期间冻结状态栏计时，并从任务总耗时和单域名查询耗时中扣除暂停区间；恢复后从暂停前数值继续
- 重查受理后立即清空目标行旧的查询时间与查询耗时；worker 开始时实时写入本次查询时间，完成后再填入本次耗时
- 修复暂停按钮在任务启动竞态中被重置，以及 RDAP 完成后仍继续进入 DNS/HTTP 阶段的问题；暂停现在会在当前同步网络请求返回后的下一个检查点生效
- RDAP/WHOIS 成功日志不再提前宣称开始 DNS；`clientHold/serverHold` 直接判定停止解析并跳过 DNS
- 排队中的查询时间和查询耗时默认留空，不再把缺失耗时格式化为 `0.00 秒`
- 注册数据查询改为默认 RDAP 优先：使用 IANA Bootstrap 自动发现各 TLD 的权威服务，离线公共后缀规则将子域归一化为可注册域，RDAP 不支持或异常时自动回退 WHOIS
- 查询期间的暂停/继续与终止控件移入结果状态条并随页面悬浮；混合演示不再切换快速模式，保持默认不限流
- 明确并补测任务超时会传递到 WHOIS、RDAP、DNS 与 HTTP 的每次网络尝试；架构流程图改用 Mermaid
- DNS 超时或异常时自动复查一次；仍未知会明确建议重查，并纳入“重新查询异常/未知”批量操作
- 新增默认的不限流查询模式与任务级超时入口，移除结果工具栏中和表头复选框重复的全选/取消按钮
- 修复 `.com/.net` 子域被注册局 404 误判为未注册：WHOIS/RDAP 改查可注册主域，DNS 仍检查原输入；
  详细结果新增 WHOIS 原始文本或 RDAP JSON，并随历史记录持久化
- 修复服务重启或热重载后，从历史详情点击重查提示“任务不存在”；重查接口现在会先恢复已落库任务
- 批量查询和重查改用受 `max_workers` 控制的线程池，移除逐域名线程启动延迟；重查期间恢复
  `processing` 状态、写回历史结果，并拒绝重叠查询请求
- 重查接口严格校验数组、规范化去重并拒绝任务外域名；worker 意外异常会记录失败结果并继续完成任务
- `.com/.net` 在 WHOIS 首次网络失败时自动回退 Verisign RDAP，可靠识别 `handwerkszubehoer.com` 等 hold 域名；
  表格状态列直接区分超时、配额、网络解析、网络连接、空响应和解析错误
- 修复移动端桌面侧栏 padding 规则残留造成的大块留白，并增加紧凑页眉布局
- 移动端页眉统一为 sticky 工具栏，汉堡菜单并入页眉且随侧栏停靠方向排列，不再独立悬浮
- 移动端查询与历史记录 Tab 改为等宽居中布局
- 移除页眉中重复的“WHOIS标准查询”平台徽标，平台选择仍保留在查询配置区
- 查询中的状态动画改为固定浮层，滚动页面时仍保持可见
- 修复任务完成瞬间状态/结果并行读取导致最后一条结果偶尔缺失；WHOIS 未返回任何注册证据时
  不再误判为已注册
- 识别 WHOIS `clientHold/serverHold`，将解析状态准确显示为「停止解析（域名被封）」并跳过重复 DNS 查询；
  结果表格与历史结果新增单域名查询时间、查询耗时
- 导出过滤新增独立的「未注册」选项；进度条移入查询结果板块；域名默认点击打开网站，逐行提供醒目的重新查询按钮
- 「混合演示」更新为 `www.baidu.com`、`cartutuoficina.com`、`handwerkszubehoer.com`、
  `docs.python.org`、`2333lqbz.xyz`，并提供快速查询模式；结果表新增「详细结果」弹窗，进度区与统计区增加间距
- 收紧 WHOIS 状态判定：只有明确的未找到响应才显示「未注册」；空响应不再误报，套接字超时单列为「超时」
  并保留错误原因；查询配置的超时秒数同步用于 WHOIS、DNS 和 HTTP 探测，结果表增加独立的状态/解析筛选

## [2.6.0] - 2026-07-28

体验与信息透明度优化版本。

### 变更（性能与行为）

- **查询未注册域名不再耗时重试**：WHOIS 返回"No match/NOT FOUND"等确定性结论时立即返回
  `not_registered`（原地重试 3 次约需 10 秒 → 现在一次即返回），错误文案统一为「域名未被注册」；
  配额/网络等临时性错误仍按原策略重试
- WHOIS 请求接入 `CONFIG['timeout']` 的 socket 超时（旧版 python-whois 自动回退兼容）
- 任务状态新增 `not_registered`：前端表格显示「未注册」徽标与单独统计卡、
  日志级别为 warn、导出文案「域名未被注册」、CLI 显示 `⊘ 未注册`
- 修正 WHOIS 异常捕获顺序：`WhoisDomainNotFoundError`/`WhoisQuotaExceededError`
  是 `PywhoisError` 的子类，此前被父类捕获导致专属分支永远不生效
- 历史记录中旧的"域名不存在/WHOIS解析错误"类失败结果保持原样，新查询才会有新状态

### 新增

- **平台能力提示**：`PLATFORMS` 增加 `implemented` 与详细 `desc`；WHOIS XML / RDAP
  目前未接入专用接口（自动回落 WHOIS 标准协议），页面选择时给出常驻提示与 Toast、
  任务启动时运行日志中也会写明实际查询方式，配置面板下方新增各平台效果与信息介绍
- **页面侧边栏大纲**：查询配置/输入域名/运行日志/查询结果/历史记录 五项锚点跳转，
  支持收起为悬浮按钮，收起状态保存到 localStorage；窄屏（<1250px）自动隐藏

### 测试

- 新增 5 例：未注册快速返回与"不重试"保证、配额错误仍重试、平台元信息完整性、
  导出 not_registered 文案；现共 43 例

## [2.5.0] - 2026-07-28

工程化重构版本：**行为不变**，重点为可维护性、可测试性与文档。

### 变更（结构）

- 将单文件 `app.py`（约 1000 行）拆分为 `domain_checker` 包：
  `settings`（配置/持久化）、`state`（内存任务状态）、`domains`（解析校验）、
  `checker`（WHOIS/DNS/流水线）、`db`（SQLite）、`tasks`（任务编排）、`export`（报表）、
  `web`（Flask 工厂与 API）；`app.py` 仅保留启动入口
- 依赖方向 `web → tasks → checker → settings/state`，无循环导入
- 版本号统一由 `domain_checker.__version__` 驱动（启动横幅）；界面标题同步 v2.5
- 新增环境变量：`DOMAIN_CHECKER_DATA_DIR/DB/HOST/PORT/DEBUG`（见 README 配置表）
- 导出目录改用 `tempfile.gettempdir()`，不再硬编码 `/tmp`

### 新增

- `tests/`：38 个 pytest 用例（网络调用全部 mock，离线可跑；数据目录自动隔离）
- `pyproject.toml`：打包元数据 + ruff/pytest 配置；`requirements-dev.txt`
- CI：GitHub Actions（Python 3.10–3.12，ruff + pytest）
- 文档：重写 README；新增 `docs/ARCHITECTURE.md`（架构/数据流/并发模型/扩展指南）、
  `docs/API.md`（HTTP API 参考）、`AGENTS.md`（代码助手接手指南）、
  `CONTRIBUTING.md`、`CHANGELOG.md`、`LICENSE`（MIT）

### 修复

- `cli.py` 遗留的未使用导入/变量；false 比较改为 `is False`（语义不变）

## [2.4.0] - 2026-07-28

### 修复

- **历史记录无法加载**：`switchTab` 依赖隐式全局 `event`，异步调用时抛错导致
  "加载失败"且无法切回查询页；改为 `data-tab` 属性切换；查看历史时自动展示结果卡片、
  设置当前任务并附带结果条数提示
- **`cli.py` 无法启动**：修正不存在的 `process_domain` 导入

### 新增

- 配置面板新增「允许局域网访问」开关：保存后持久化到 `data/settings.json`，
  重启后生效；页面显示服务器实际监听状态；启动横幅打印当前模式与局域网地址

### 变更（文案更准确）

- DNS 返回 NXDOMAIN 不再显示"域名不存在"：改为"已注册但无解析记录，疑似已被停止解析/冻结"
  （NoAnswer/NoNameservers/Timeout 均有对应准确文案，超时单列为"未知"状态徽标）
- 前后端统一用词：解析列徽标"未解析"、统计"未解析"、导出列"解析异常原因"

## [2.3.0] - 2026-07-27

初始发布基线：批量 WHOIS 查询、暂停/继续/取消、单条与批量重查、多主题、表格排序、
历史记录（SQLite）、CSV/Excel 导出、URL 自动提取域名、限流与自动重试、多平台选择界面。

[Unreleased]: https://github.com/xuchaoxin1375/domain-checker/compare/v2.6.0...HEAD
[2.6.0]: https://github.com/xuchaoxin1375/domain-checker/compare/v2.5.0...v2.6.0
[2.5.0]: https://github.com/xuchaoxin1375/domain-checker/compare/v2.4.0...v2.5.0
[2.4.0]: https://github.com/xuchaoxin1375/domain-checker/compare/v2.3.0...v2.4.0
[2.3.0]: https://github.com/xuchaoxin1375/domain-checker/releases/tag/v2.3.0
